<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title><%= title %></title>
</head>
<body>
    <%- include('./head.ejs')  %>

    <section class="home">
        <a href="/catalog/brand/create" class="page-links">New Brand</a>
        <%if(brand.length > 0){%>
        <%brand.forEach(element => {%>
            <div class="home-page">
                <a href="/catalog/brand/<%=element._id%>" class="page-links"> <%=element.name %> </a>
                <p> <%= element.origin%> </p>
            </div>
       <%})%>
       <%}else{%>
            <p>There are no Brands</p>
        <%}%>
    </section>
</body>
</html>
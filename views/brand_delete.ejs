<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title><%= title %></title>
</head>
<body>
    <%- include('./head.ejs')  %>
    <section class="home">
        <div class="home-page">
            <p class="home-info title"><%=brand.name %></p>
            <p class="home-info"><%=brand.origin %></p>
        </div>
        
        <% if(shoes.length > 0) { %>
            <div class="home-page">
                <p class="home-info title">Delete the following shoes before deleting this brand.</p>
                <%shoes.forEach(element => { %>
                    <a class="page-links" href="<%= element.url %>"><%= element.title %></a>
                    <p class="home-info"><%= element.description %></p>
                <% }) %>
            </div>
            
        <% }else { %>
            <div class="home-page">
                <p class="home-info title">Are you sure you want to delete this brand?</p>
                <form method="POST">
                    <input type="hidden" name="brandid" value="<%=brand._id %>">
                    <button type="submit">Delete</button>
                </form>
                </div>
            
        <% }; %>
    </section>
    
</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title><%= title %></title>
</head>
<body>
    <%- include('./head.ejs')  %>
    <section class="home">
        <div class="detail-links">
            <a class="page-links" href="/alltypes/<%=type.id  %>/update">Update Type</a>
            <a class="page-links" href="/alltypes/<%=type.id  %>/delete">Delete Type</a>
        </div>

        <div class="home-page">
            <p class="home-info title">Type Information</p>
            <p><b>Name:</b>  <%= type.name %></p>
        </div>

        <div class="home-page">
            <p class="home-info title">Shoes</p>
            <%if(shoes.length > 0){%>
                <%shoes.forEach(element => {%>
                    <div class="shoe-list">
                        <p class="home-info"><%= element.name %></p>
                    </div>
               <%})%>
               <%}else{%>
                    <p>There are no shoes for this type</p>
                <%}%>
        </div>
    </section>

</body>
</html>